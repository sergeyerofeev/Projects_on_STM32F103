#### Код для получения данных с линии UART

#### Подключение
>PA10 (UART1) подключаем к линии Rx
>PB11(UART3) подкючаем к линии Tx
>PA2 (UART2) вывод Tx для передачи данных в терминад, подключаем к Rx на USB-TTL модуле

___